# 🌙 Nuit du 19 au 20 juillet 2026

Fenêtre : 22:09 UTC → ~02:15 UTC (00:09 → ~04:15 à Paris). Session prolongée à
la demande de François (« utilise 100 % des tokens ») — deuxième partie de nuit
consacrée aux pièges IRI/Circana, au mapping vérifié par attendus, et aux
bugs de niveau données (lignes de total).

## Testé
- 1 fichier réel (inbox) : `test-D-entetes-exotiques.xlsx` — 0 champ critique
  manquant, mais **mauvais mapping détecté** : « Code article » (alias
  substring `article`) volait le champ *nom produit* à « Désig. », donnant des
  noms type « RB-001 » au lieu du libellé réel.
- 13 nouveaux cas de corpus synthétiques (`training-data/corpus/`) : abréviations
  FR (Désig., Réf., Fabr., Mq, Qtés, UVC, PV TTC/moyen), intitulés EN
  Nielsen/Circana (Vendor, Supplier, Item Description, Dollar Sales, Unit
  Sales, NPD), pièges de mise en forme (en-têtes en ligne 3, casse/accents,
  colonnes parasites Période/Zone/Commentaire, CSV point-virgule cp1252,
  apostrophe typographique « d’ »).
- Avant enrichissement : 5 fichiers en échec critique. Après : **0 échec sur
  tout le corpus + inbox**, `npm run build` OK.

## Appris
- Alias ajoutés (en-tête → champ) :
  - `vendor`, `supplier`, `mq` → marque
  - `desig.`, `ref.`, `description` → nom produit (l'alias exact `desig.`
    corrige aussi la priorité sur test-D : le match exact passe avant le
    substring `article`)
  - `dollar sales` → CA ; `uvc`, `unit sales` → volume
  - `pv ttc`, `pv moyen` → prix ; `marche` → segment ; `npd` → nouveauté
- Correctif générique démontré par un cas : normalisation des apostrophes
  typographiques (’ ‘ → ') dans `parse.ts` et `train-parser.mjs` — sans quoi
  « Chiffre d’affaires » (apostrophe Excel) ne matchait pas.
- Constat CSV (non corrigé, à trancher) : app et script lisent les CSV en
  windows-1252 sauf BOM UTF-8 (comportement SheetJS). Les exports Excel réels
  sont cp1252 ou UTF-8-avec-BOM → OK. Un CSV UTF-8 **sans** BOM afficherait des
  accents cassés dans les en-têtes. Cas jugé rare, aucun fichier réel ne le
  démontre encore.

## Deuxième partie de nuit (00:30 → 04:15 Paris)

### Testé
- 10 nouveaux cas de corpus (total : 23) : pièges parts de marché EN/FR
  (« Value Share % », « PDM valeur », « % ACV », « DN », « DV », « Poids CA »,
  « Weighted Distribution »), piège de l'alias court (« Catégorie » capturée
  par « ca »), en-têtes multilignes (retour à la ligne Excel), classeur
  multi-feuilles avec page de garde « Sommaire », IRI classique
  (« Base Dollar Sales », « Avg Unit Price »), ligne « Total catégorie »,
  CSV cp1252 avec milliers à l'espace (« 1 234 567 »).
- Preuve avant/après : l'ancienne détection donnait CA = « Catégorie »
  (fr-categorie-ca-trap), CA = « PDM valeur » (fr-pdm-trap), et rien du tout
  sur le multi-feuilles (page de garde lue). Tout passe après correctifs.
- Vérification en conditions réelles (Playwright, upload dans l'app) :
  multi-feuilles → planogramme généré ; ligne de total → exclue avec
  avertissement « 1 ligne(s) de total/agrégat ignorée(s) » ; CSV milliers →
  parts CA exactes 56 % / 44 %. Jeu d'exemple : aucune régression.

### Appris
- **Harnais renforcé** : fichiers d'attendus `training-data/expect/<tag>/
  <fichier>.expect.json` — le script vérifie désormais que chaque champ est
  mappé sur la BONNE colonne (mapping incorrect = échec critique). 13 fichiers
  pinés, harnais testé par corruption volontaire d'un attendu (échec bien
  détecté, exit 2).
- **Détection** (parse.ts + script, en phase) :
  - colonnes de parts/distribution (share, PDM, % ACV, DN, DV, poids,
    weighted, distribution) exclues des champs numériques (CA, volume, marge,
    prix) ;
  - alias courts (≤ 2 lettres, ex. « ca ») exigent une frontière de mot —
    « CA (K€) » matche toujours, « Catégorie » plus jamais ;
  - normalisation des espaces (en-têtes multilignes « Ventes\nvaleur ») ;
  - choix de feuille au score de détection (page de garde ignorée, 8
    premières feuilles examinées).
- **Niveau données** (parse.ts) : lignes d'agrégats (« Total », « Sous-total »,
  « Grand total », « Ensemble ») exclues des produits avec avertissement —
  avant, « Total catégorie » devenait la première marque du plan de masse.
- **Vérifié sans correctif nécessaire** : toNumber gérait déjà les milliers à
  l'espace (y compris insécables) — contrôlé avant de « corriger » du code sain.

## Troisième vague (fin de nuit)
- 4 cas de vocabulaire sell-out : « Sell-out valeur/volume » (passait déjà,
  piné), « Ventes (€) » / « Ventes (UVC) », « Montant », « Net Sales » —
  3 échecs avant, 0 après.
- Alias ajoutés : `sell-out`, `sell out`, `montant`, `net sales`, `ventes €`
  → CA. Parenthèses traitées comme séparateurs dans la normalisation
  (« Ventes (€) » ≡ « ventes € »), démontré par le cas fr-ventes-euro et
  vérifié en app (parts CA 60/40 exactes).
- Attendu du piège catégorie mis à jour : « Sell-out net » est désormais
  reconnu comme CA (le fichier devient entièrement exploitable).
- Corpus final : 27 cas, 19 attendus de mapping pinés, 0 échec, build OK.

## Sondes de fin de nuit
- En-têtes sur 2 lignes (« Ventes » fusionné au-dessus de valeur/volume) :
  fonctionne déjà (la ligne pauvre en texte est sautée) — cas piné
  (fr-entetes-2-lignes).
- Multi-périodes (fr-multi-periodes, « CA P6 / CA P7 / Qté P7 ») : INCOHÉRENCE
  détectée — le CA est pris sur P6 (première colonne qui matche) mais le
  volume sur P7. Non corrigé volontairement : la règle de choix (dernière
  période ? colonne « N » vs « N-1 » ?) est une décision produit à valider
  avec François avant d'être codée. Cas laissé au corpus SANS attendu.

## Objectif de la prochaine session
- Trancher avec François la règle multi-périodes, puis l'implémenter et
  pinner fr-multi-periodes.
- Si un fichier réel apparaît dans training-inbox/ ou un signalement dans
  parse_reports (clés Supabase absentes cette nuit) : priorité absolue.
